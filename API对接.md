# A（渲染/底层）与 B（应用/UI）之间的对接 API 设计

本文列出 A（渲染/底层）与 B（应用/UI）之间明确需要对接的 API，  
重点是双方交互的函数，而不是每个人内部实现的细节。

这些接口是双方协作、数据沟通的关键点。

---

## 1. 初始化阶段的接口

### B → A：初始化渲染器

```cpp
bool InitRenderer(int width, int height);

